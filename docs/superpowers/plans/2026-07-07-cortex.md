# CORTEX (v0.4.0) + Breadth Pack (v0.5.0) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans (inline). This plan executes the two committed specs, which carry the full implementation detail — every formula, algorithm, file, and test case:
> - `docs/superpowers/specs/2026-07-07-cortex-design.md` (the judge-reconciled master design; §8 is the canonical file map + build order, §9 the test plan)
> - `docs/superpowers/specs/2026-07-07-breadth-pack-design.md`
> The specs are the source of truth; this plan is the execution checklist. User pre-approved the slate and granted full autonomy through ship.

**Goal:** Onyx becomes a complete local-first second brain: three-mode shell (BRAIN / DASHBOARD / SKILLS), Iron-Man HUD chrome with boot sequence + status bar, analytics dashboard on a daily snapshot store, CORTEX skill tree (7 branches / 34 skills, real predicates), command palette, daily notes, quick capture, pins, tasks — then the breadth pack (backlinks, templates, SRS flashcards, focus/pomodoro, resurface, reading list, habits).

**Tech:** existing stack only; zero new npm dependencies across both slices.

## Global constraints
- Follow CORTEX spec §2.6 z-ladder, §3.7 motion rules, §3.8 blur budget, §5.2 frozen counter namespace verbatim.
- index.css additions are append-only banner sections; App.jsx gets ONE structural edit (milestone C2), single-line additions after.
- All pure libs `.mjs` + `node --test`; TESTS GREEN gate before every commit (`npm test` exit code, not grep).
- Screenshot-verify (ONYX_SHOT harness) at the end of C2, C3, C4, C5 and each breadth feature with UI.
- Branch `feat/cortex`; ship v0.4.0 after C5 review; branch `feat/breadth` after; ship v0.5.0 after B-slice review. Merge+tag per established release flow.

## Milestones

### C1 — Foundation (releasable silently as 0.3.3)
- [ ] `src/main/appdata.js` (usage + snapshots per spec §5.1/§4.2; debounced flush + before-quit)
- [ ] 6 IPC channels + preload methods (§7.2); `ensureNote` in vault-indexer (create-only `wx`)
- [ ] config DEFAULTS + `pins`, `dailyFolder: '06 - Daily Logs'`
- [ ] indexer: `note.tasks` via tasks.mjs
- [ ] Pure libs: `bus.mjs`, `hud.mjs`, `fuzzy.mjs`, `daily.mjs`, `tasks.mjs`, `dashboard.mjs`, `skills.mjs`; `stats.mjs` maturity v2 + `dayKey`; `clusters.mjs` CLUSTER_PALETTE export
- [ ] Tests: hud/bus/fuzzy/daily/tasks/dashboard/skills + stats/indexer extensions (§9)
- [ ] snapshot-on-reindex + vaultEdit bump live in main

### C2 — Chrome
- [ ] chrome.jsx (useCountUp/Num/Kbd), tokens + chrome-kit CSS (.u-label/.brk/.rule-ticks/.sdot/.rule-progress/.panel-in/.num/[data-tip]/.kbd), global scanlines
- [ ] BootSequence, TopBar (mode tabs + SKILLS·LV + search button + LENS), StatusBar (useFps DOM-write pattern), Toasts, Section, HoverLayer + bus reroute
- [ ] setPaused on all 5 views; SpaceCanvas `paused` prop; `.stage.dimmed`
- [ ] THE App.jsx structural edit (§2.4) + keyboard contract (§2.5)
- [ ] Screenshot: boot mid+post, brain mode chrome, placeholder modes

### C3 — Modes
- [ ] DashboardMode + Heatmap + Gauge `size` prop (panels per §4.4)
- [ ] SkillsMode constellation + skillcard + unlock-diff effect + toasts
- [ ] Screenshots: dashboard, skills (via __onyxDebug.setMode)

### C4 — Tools
- [ ] CommandPalette + focus(id) fly-to; QuickCapture; openDaily + reader daily nav; pins end-to-end; TasksPanel (sidebar+dashboard); all §5.2 counter bumps wired
- [ ] Screenshots: palette, capture, pinned sidebar

### C5 — Polish + ship v0.4.0
- [ ] Cockpit/HudSidebar/HudToolbar restyle to chrome kit; metric de-dup audit; skill-threshold calibration vs real vault; reduced-motion pass
- [ ] Full screenshot sweep (3 modes, boot, palette, capture, 1x/1.5x scanline moire)
- [ ] Multi-agent review workflow → fixes → merge → v0.4.0 tag

### B — Breadth pack (order per breadth spec) → v0.5.0
- [ ] B1 backlinks+intel · B2 focus+pomodoro (+store.js) · B3 templates · B4 resurface · B5 SRS flashcards · B6 reading list (+shell IPC) · B7 habits
- [ ] Tests per breadth spec; screenshots per feature; review workflow → merge → v0.5.0 tag

## Reporting
Maintain a running CHANGES summary for the user's return: every milestone commit, every screenshot verdict, every judgment call made autonomously.
